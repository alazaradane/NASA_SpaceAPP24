import React from 'react'

const page = () => {
  return (
    <div>
    <iframe
        src="https://ultimatum.bettermode.io/"
        width="100%"
        height="800"
        frameBorder="0"  // Fixed the casing
        scrolling="auto"
        allowFullScreen   // Fixed the casing
      ></iframe>
    </div>
  )
}

export default page